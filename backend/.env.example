# PostgreSQL (Neon)
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Encryption key for merchant address storage (AES-256-GCM)
ENCRYPTION_KEY=your-64-char-hex-key

# Server
PORT=3000
